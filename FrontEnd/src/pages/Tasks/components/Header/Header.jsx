import './Header.css';

/**
 * Renders the header component for the Task Manager application.
 *
 * @returns {JSX.Element} A heading element displaying "Task Manager"
 */
function Header(){
    return (
        <h1 className="task__title" >Task Manager</h1>
    )
}

export default Header;